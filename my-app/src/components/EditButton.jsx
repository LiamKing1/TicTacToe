import { useState } from "react";

function EditButton() {
  const handleEdit = () => {};

  return (
    <>
      <button onClick={handleEdit}> Edit </button>
    </>
  );
}

export default EditButton;
